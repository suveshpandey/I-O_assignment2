//WAP for finding the volume of the cylinder by taking radius and height as input.
#include<iostream>
using namespace std;
int main(){
    float h,r;
    cin>>h>>r;
    float volume = (3.14)*r*r*h;       // volume = π · r2 · h
    cout<<volume;

}
